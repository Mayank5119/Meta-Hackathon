# Core API server
fastapi
uvicorn[standard]
pydantic
httpx
pyyaml

# LLM baseline
openai

# Local dev / agent / UI - NOT needed in Docker
# See requirements-dev.txt for: torch, numpy, gradio, matplotlib, pandas