# -- Core RL & Web --
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.7.0
httpx>=0.27.0
pyyaml>=6.0.1
networkx>=3.3

# -- LLM --
openai>=1.35.3

# -- Data Science & RL (Requires latest versions for 3.14 compatibility) --
# Note: These may still fail to compile without a C++ Build Toolchain
torch>=2.3.0
numpy>=2.0.0
pandas>=2.2.2
matplotlib>=3.9.0

# -- Image Processing --
# We use >= here because 10.3.0 specifically fails on 3.14
pillow>=10.4.0

# -- UI & Utilities --
gradio>=4.36.0
requests>=2.32.0